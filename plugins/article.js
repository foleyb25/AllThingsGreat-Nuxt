import { useArticleStore } from '@/stores/article.store';

export default defineNuxtPlugin(nuxt => {
    // const store = useArticleStore(nuxt.$pinia);

    // addRouteMiddleware('my-middleware', async (to, from) => {
    //     console.log("IN MY Middleware")
    // }, { global: false });
});